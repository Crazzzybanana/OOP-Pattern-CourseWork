using System; namespace OOP_Course_Work.Deliveryman {     class SteveHandler : DeliverymanHandler     {         public override void Handle(Receiver receiver)         {             if (receiver.DeliverymanSteve == true)                 Console.WriteLine("Order will be delivered by Steve");             else if (Successor != null)                 Successor.Handle(receiver); 			else Console.WriteLine("No deliveryman avaliable at the moment");         }     } } 